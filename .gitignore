node_modules
dist
dist-ssr
*.local

# Environment / secrets — never commit real values.
# Matches .env, .env-local, .env.production, etc. — but keep the example.
.env*
!.env.example

# TypeScript / build artifacts
*.tsbuildinfo
# Emitted by `tsc -b` for the node-side config project (vite.config.ts).
vite.config.js
vite.config.d.ts

# Tooling / editor / OS
.claude/
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.log
npm-debug.log*
